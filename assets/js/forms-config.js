/* ============================================================================
   FMLG lead-form configuration
   ----------------------------------------------------------------------------
   Paste your Zapier "Catch Hook" URL between the quotes to start delivering
   form submissions to Smokeball (Create Lead). While this is empty, every form
   still validates and advances to the thank-you page — nothing is sent.
   ============================================================================ */
window.ZAPIER_LEAD_WEBHOOK_URL = "";
